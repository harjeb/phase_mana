{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "setup",
  "description": "Only the bundled setup page may receive setup progress; remote localhost gameplay has no IPC capability.",
  "local": true,
  "windows": ["setup"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
